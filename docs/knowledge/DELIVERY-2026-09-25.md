# Knowledge recovery delivery — 25 September 2026

## Scope

Preservation and explicit continuation, not a production radio change. Parent: f6b09a492c362f1c73b775d67721da1cc345fe91 on EmotiveImpact/loc8 main. Intended recovery ref: archive/loc8-knowledge-2026-09-25. See the associated PR and remote ref for the resulting commit; this file cannot contain its own eventual commit SHA.

New knowledge entry point, preservation protocol, source-applicability ledger, four transfer packs, original-byte inventory, dated copies of the 48-repository provenance/source-audit ledgers and September R&D programme, and an isolated observation/delivery reference. AGENTS.md retains the exact Expo 57 requirement and appends continuity instructions. Existing production files, package manifests, lockfiles and native code are unchanged.

## Verification actually run

`node --test tools/knowledge-reference/contracts.test.mjs` on Node v22.16.0: 36 tests passed, zero failed/skipped/cancelled. Synthetic inputs only. Checks cover delayed-arrival/reconnect freshness, age boundaries, unknown/future/rollback clocks, missing/invalid positions, withheld visibility, separate queue/submission/relay/destination/human evidence, correlation rejection, duplicate and conflicting event IDs, and input-copy behaviour.

No complete monorepo dependency installation, TypeScript compiler run, Jest suite, application build, native build, physical relay test or deployment was performed by this recovery increment. No CI-green claim. The earlier R0 80-check result is historical and separate. Its source is draft PR #1; it was not merged or rerun here.

## Original-artifact preservation status

Eight original Library files were materialised and hashed; their exact identities, byte lengths and SHA-256/Git-blob hashes are in RECOVERY-INVENTORY.json. All eight are included byte-for-byte in the conversation recovery ZIP.

The September R&D programme is also archived as a raw Markdown file in this branch. The arena PDF is already byte-identical to the existing tracked docs/research/arena-dossier-2026-07-21.pdf. Four other original Markdown handovers and two standalone HTML previews are preserved in the conversation ZIP but are not claimed as byte-for-byte uploads in this initial repository change; their findings and integration boundaries are consolidated into the tracked knowledge/transfer documents. This is a known preservation remainder, not silently counted as completed upload.

No complete copy of the ignored 48 upstream clones, separate local Command source repository/history or raw weekly conversation archive was recovered. A metadata ledger is not a source-code backup. The owner's local disk and reflogs were not inspected, so no deletion cause is asserted.

## Remaining work

Archive the remaining exact Library originals into durable repository/release storage; recover any still-existing Command local source/history non-destructively; reconstruct only specifically needed upstream revisions from the existing licence-aware provenance ledger; review this preservation PR; validate R0 in the exact repository environment; then integrate the observation-freshness invariants into existing host records/selectors and UI. Preserve the connected-pilot and mapping lanes alongside physical mesh proof.

No messages have been inserted into other conversations and no other product repository has been modified. TRANSFER-PACKS.md contains the copyable receiving-session instructions and acceptance gates.
